import React from 'react';

const Error = () => {
    return (
        <h1 style={{ color: 'red', marginTop: 30 }}>
            You moved to page that doesn't exist.
        </h1>
    )
};

export default Error;